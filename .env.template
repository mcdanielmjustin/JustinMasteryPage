# Patient Encounter Generator — API Key Setup
# ─────────────────────────────────────────────
# Copy this file to .env and fill in your API key:
#   cp .env.template .env
#
# Then run the full generator:
#   python generate_presentations.py --all --count 30 --resume
#
# This will expand each domain from 2 seed encounters to 30,
# writing incrementally after each batch of 5 so progress is saved.
#
# Per-domain estimates: ~30 min, ~$2–4 per domain (Opus 4.6 pricing).
# Full run (9 domains × 30 encounters): ~4–6 hrs, ~$20–35.
#
# Use --resume to pick up from where you left off after interruptions.
# Existing encounters are never overwritten.

ANTHROPIC_API_KEY=sk-ant-api03-REPLACE_WITH_YOUR_KEY
